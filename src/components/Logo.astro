---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class={`font-serif ${className}`.trim()}>
  <div class="text-logo">
    <span>JPH</span><span class="text-accent">A</span>
  </div>
  <div class="text-logo-sub -mt-2 text-right text-[#1f365c]">Heidelberg</div>
</div>
